int bubblesort(int arr[] ,int n){
for(int i=0;i<n;i++){
boolean swapped=false;  //for no need to run loop when array is already sorted
 for(int j=0;j<n;j++){
    if(arr[i]>arr[j]){
      swapped=true;
      swap(arr[i],arr[j]);
}
}
if(swapped==false){
break;
}
}
}
